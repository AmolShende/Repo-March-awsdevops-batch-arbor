apiVersion: v1
kind: Service
metadata:
  name: myrc-service    #Name of Service
spec:
  selector:
    app: myapp      # Selector label = This service will manage POD with these labels
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 30080
  type: NodePort
